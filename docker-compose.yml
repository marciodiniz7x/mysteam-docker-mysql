version: '3'
services:
  app:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:var/www/html
    depends_on:
      - db

  db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: "" 
      MYSQL_DATABASE: mysteam
      MYSQL_USER: root
      MYSQL_PASSWORD: ""